//todo item factory function
let Item = () => {
    return {title, description, creationDate, dueDate, isCreated, parentProject};    
    // return {title, description, dueDate, creationDate, parentProject}
};

let display = (() => {

    let _projects = {
        defaultProject: []
    }

    const _initialize = () => {
        document.getElementById('addItem');

        //getproject
    };









}) ();